// secrets.h.example  — template, SAFE to commit.
//
// Copy this file to "secrets.h" and fill in your real WiFi credentials.
// secrets.h is gitignored, so your password never goes to GitHub.
//
//   cp secrets.h.example secrets.h
//   # then edit secrets.h
#ifndef SECRETS_H
#define SECRETS_H

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASS "YOUR_WIFI_PASSWORD"

#endif // SECRETS_H
